import React, { Fragment } from 'react';
import Landing from '../components/layout/Landing';

const Home = () => {
  return (
    <Fragment>
      <Landing></Landing>
    </Fragment>
  );
};

export default Home;
